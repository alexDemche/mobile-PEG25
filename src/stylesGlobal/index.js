import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  flatList: {
    alignItems: 'center',
    paddingHorizontal: 10,
    paddingVertical: 10,
    marginHorizontal: 10,
    marginVertical: 20,
  },
});
